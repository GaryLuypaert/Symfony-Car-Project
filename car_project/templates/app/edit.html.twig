{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier la voiture</h1>

    <div class="container">
        {% include("app/form.html.twig") %}

        <button class="addKeyword btn btn-info">Ajouter mots-clés</button>

        <button type="submit" class="btn btn-success">Modifier</button>

        {{ form_end(form) }}
    </div>

{% endblock body %}

{% block javascripts %}

    {{ parent() }}

    <script src="{{ asset('js/form_car.js') }}"></script>


{% endblock javascripts %}