{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}

{#      <button type="submit" class="btn btn-success">Enregistrer</button>#}

        {{ form_end(form) }}
    </div>
{% endblock %}